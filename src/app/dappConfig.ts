import { FullConfig } from "@usedapp/core";
// import { CHAIN } from "app/chain";

const config: Partial<FullConfig> = {
  // readOnlyChainId: CHAIN.id,
  // readOnlyUrls: CHAIN.rpcUrls,
};

export default config;
